# include <stdio.h>
# include <math.h>

int main () 
{
    int a, b, result;
    printf("Enter two numbers.\n");
    scanf("%d%d", &a,&b);
    result = pow(2,3);
    printf("The result is %d.\n", result);

    return 0;
}